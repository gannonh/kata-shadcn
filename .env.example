# Secret token protecting /r/* registry endpoints.
# Set this in Vercel project environment variables.
# Consuming projects add this to their .env and reference it in components.json headers.
REGISTRY_TOKEN=your-secret-token-here

# Optional override for the unscoped dependency proxy route:
# /styles/{style}/{name}.json -> {SHADCN_FALLBACK_REGISTRY_URL}/styles/{style}/{name}.json
# Defaults to https://ui.shadcn.com/r
SHADCN_FALLBACK_REGISTRY_URL=https://ui.shadcn.com/r
