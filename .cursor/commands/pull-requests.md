# Pull requests

Run the pull-requests skill and take the following workflow: open a PR from the current branch, populate the description with summary, impacted paths, verification commands/results, linked issue, and UI screenshots when relevant.
